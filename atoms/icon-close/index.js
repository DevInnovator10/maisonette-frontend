/** X icon in svg to be used in new shareable wishlist,
 * following the new design and
 * the standards of our codebase
*/
import styled from '@emotion/styled';
import svg from '../../public/images/close-icon.svg';

const Icon = styled(svg)``;

Icon.whyDidYouRender = true;

export default Icon;
